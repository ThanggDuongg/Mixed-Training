string Convert(string s, int numRows)
{
    var len = s.Length;
    char[][] zigZagArr = new char[numRows][];
    for (int i = 0; i < numRows; i++)
    {
        int j = 0;
        while (true)
        {
            zigZagArr[i][j] = 
        }
    }
}